
{{#each posts as |post|}}
<div class="container-fluid">
    <div class="post-header">
    <h2>{{post.title}}</h2>
    <h3>Posted by {{post.user.name}} on {{format_date post.date_created}}</h3>
    </div>
    <p>{{post.text}}</p>
</div>
{{/each}}